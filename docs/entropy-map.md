<!-- generated by scripts/build_entropy_map.py -->

# Entropy Map

The threat matrix for the v1.5.0 capability surface. This file is generated from `ACTION_WHITELIST` and `LEGAL_TEXT_CODES` by `scripts/build_entropy_map.py`. Do not hand-edit; the build marker on line 1 is required.

## Capability whitelist

`ACTION_WHITELIST` has **26** entries, below the v1.5.0 cap of 32 (`CAPABILITY_BUDGET_V150`; the v1.4.0 cap of 24 is preserved as `CAPABILITY_BUDGET_V140`). The cap is enforced by `scripts/invariants_check.ps1`.

| Capability | input-class | actor tier | risk | rationale |
|---|---|---|---|---|
| `attachment.delete` | bytes (binary) or string (text/SVG); MIME-routed | `loopback` | MEDIUM | attachment.delete on the loopback C1 surface; whitelisted. |
| `attachment.get` | bytes (binary) or string (text/SVG); MIME-routed | `loopback` | LOW | attachment.get on the loopback C1 surface; whitelisted. |
| `attachment.put` | bytes (binary) or string (text/SVG); MIME-routed | `loopback` | MEDIUM | attachment.put on the loopback C1 surface; whitelisted. |
| `branch.create` | path params + JSON body (parent_message_id) | `loopback` | MEDIUM | branch.create on the loopback C1 surface; whitelisted. |
| `branch.list` | path params + JSON body (parent_message_id) | `loopback` | LOW | branch.list on the loopback C1 surface; whitelisted. |
| `branch.switch` | path params + JSON body (parent_message_id) | `loopback` | MEDIUM | branch.switch on the loopback C1 surface; whitelisted. |
| `chat.send` | free-text (user-supplied; non-legal bytes escaped at audit render) | `loopback` | MEDIUM | chat.send on the loopback C1 surface; whitelisted. |
| `config.get` | JSON object (validated against per-config schema) | `loopback` | MEDIUM | config.get on the loopback C1 surface; whitelisted. |
| `config.set` | JSON object (validated against per-config schema) | `loopback` | MEDIUM | config.set on the loopback C1 surface; whitelisted. |
| `eval.run` | path params + JSON body | `loopback` | HIGH | eval.run on the loopback C1 surface; whitelisted. |
| `llm.health` | none (read-only health check) | `loopback` | LOW | llm.health on the loopback C1 surface; whitelisted. |
| `manifest.read` | path params | `loopback` | LOW | manifest.read on the loopback C1 surface; whitelisted. |
| `model.list` | path params + JSON body | `loopback` | LOW | model.list on the loopback C1 surface; whitelisted. |
| `model.read` | path params + JSON body | `loopback` | LOW | model.read on the loopback C1 surface; whitelisted. |
| `plugin.list` | path params + JSON body | `loopback` | LOW | plugin.list on the loopback C1 surface; whitelisted. |
| `plugin.load` | path params + JSON body | `loopback` | HIGH | plugin.load on the loopback C1 surface; whitelisted. |
| `plugin.unload` | path params + JSON body | `loopback` | MEDIUM | plugin.unload on the loopback C1 surface; whitelisted. |
| `prompt.read` | path params | `loopback` | LOW | prompt.read on the loopback C1 surface; whitelisted. |
| `secret.delete` | bytes (lossless; v0x04 envelope) | `loopback` | HIGH | secret.delete on the loopback C1 surface; whitelisted. |
| `secret.list` | bytes (lossless; v0x04 envelope) | `loopback` | MEDIUM | secret.list on the loopback C1 surface; whitelisted. |
| `secret.put` | bytes (lossless; v0x04 envelope) | `loopback` | MEDIUM | secret.put on the loopback C1 surface; whitelisted. |
| `session.create` | path params + JSON body | `loopback` | LOW | session.create on the loopback C1 surface; whitelisted. |
| `session.delete` | path params + JSON body | `loopback` | MEDIUM | session.delete on the loopback C1 surface; whitelisted. |
| `session.list` | path params + JSON body | `loopback` | MEDIUM | session.list on the loopback C1 surface; whitelisted. |
| `session.patch` | path params + JSON body | `loopback` | MEDIUM | session.patch on the loopback C1 surface; whitelisted. |
| `session.read` | path params + JSON body | `loopback` | MEDIUM | session.read on the loopback C1 surface; whitelisted. |

## Legal charset (Number Gate, ADR-0016)

The audit-render boundary uses a **62**-code alphabet: `[A-Z a-z 0-9]`. The other **194** byte codes (out of 256) are replaced with `#0xNN` escape tokens in the audit log.

The filter applies at the **audit-render boundary only**. The in-memory `SessionEvent.payload` and the v0x04 envelope storage are lossless. The provider boundary reconstructs the exact original bytes via `from_codes(strict=False)`.

## Filter domains (ADR-0021)

Every byte flow in the system is in exactly one of four domains. The 62-gate is a render-time projection; it applies to `audit-text` only.

| Domain | Rule | Where |
|---|---|---|
| `audit-text` | 62-gate, escape non-legal | C2 log emit, Entropy Map, secret names, capability names, file paths, manifest fields |
| `asset` | no filter | attachment binaries, `data:` URIs, envelope ciphertext on disk, `SessionEvent.payload` |
| `provider-bytes` | lossless via `from_codes(strict=False)` if needed | provider HTTP body, API key bytes |
| `prompt-text` | no filter (governed by C3+C9+length cap) | user message content, chat.send WS frame, C3 user section |

## Audit surface inventory

| Surface | Domain | Filter? | Reason |
|---|---|---|---|
| C1 request body (incoming) | `prompt-text` | No | User input. Lossless. |
| C1 in-memory handler state | n/a | No | In-process, never rendered. |
| `SessionEvent.payload` (in-memory) | `asset` | No | Lossless. |
| Audit log emit (`on_session_event`) | `audit-text` | Yes | Render boundary. 62-code projection. |
| `secrets.log` envelope storage | `asset` | No | v0x04 canonical, base64 sealed. |
| `~/.dhc/attachments/*` envelope storage | `asset` | No | v0x04 canonical, name-bound to session_id. |
| Provider HTTP request body | `provider-bytes` | No | `from_codes(strict=False)` reconstructs exact bytes. |
| Provider response (SSE stream) | `provider-bytes` | No | Provider output, not user-controlled. |
| `<img src="data:...">` in web client | `asset` | No | Base64 data URI; bytes are the bytes. |
| `branch.create` / `branch.switch` audit event | `audit-text` | Yes | Render boundary. 62-code projection. |
| `attachment.put` audit event | `audit-text` | Yes | Render boundary (ref + mime + size, not bytes). |
| `logger.warning` | `audit-text` | Yes | Render boundary. Same filter as audit log. |
| `docs/entropy-map.md` (this file) | `audit-text` | Yes | Render boundary. |
