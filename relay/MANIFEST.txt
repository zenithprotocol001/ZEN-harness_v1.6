DHC v1.6.0 — Relay Manifest
=============================

Ship date:    2026-09-09
DHC-V:        100.0 (production_ready)
Findings:     0 critical, 0 high, 0 medium, 0 low
Test count:   1000 passing (913 Python + 87 vitest), 2 skipped, 1 xpassed
Invariants:   100+ passing (positive + negative)
Bundled plugins: 5
SHAs pinned:  5 / 5 (see docs/SHA-PINNING.md)

What's new in v1.6.0 (over v1.5.1.5)
-----------------------------------
- **SecretSource polymorphic JSONL schema** —
  `dhc.cordis.secrets` gains `SecretSource` dataclass +
  `SecretSourceType` closed enum (raw / env / file). The
  `put_source(name, source)` API replaces `put_raw` for
  new code; `put_raw` is retained as a thin wrapper. The
  JSONL log forks: `raw` records keep the v1.5.1.4 shape;
  `env` and `file` records add `source` + `ref` + `ref_hint`
  alongside the v0x04 envelope. v1.5.1.x logs replay
  unchanged.
- **ProviderState kernel cache** —
  `dhc.services.provider_state.ProviderStateManager` is a
  per-process cache of `ProviderState` per provider. Written
  by `_api_llm_health_provider` after a probe; read by
  `GET /api/settings/state` and the React tree on mount.
- **GET /api/settings/state** — schema-driven redaction
  endpoint. Returns `{providers: {<provider>: {status,
  source, ref_hint, hint, health, health_label,
  last_check_at}}}` for the closed set of providers in
  `ModelRegistry`. NEVER echoes a raw key value.
- **IngressScrubber** — `dhc.security.ingress_scrubber.
  scrub(text)` redacts known provider key prefixes
  (`sk-or-v1-`, `sk-ant-`, `sk-proj-`) to `[REDACTED_API_KEY]`.
  Applied at TWO points (defense in depth): the WS handler
  (before journal write) and the C7 adapter (before LLM
  call). The pattern set is closed (kernel ADR required).
- **Frontend source pill** — `SettingsModal` fetches
  `/api/settings/state` on mount and renders a read-only
  pill (`env: OPENROUTER_API_KEY` or `file: openrouter.key`)
  next to the per-key input. The env/file PUT UI is a
  follow-up; the pill is display only in v1.6.0.
- **3 source-level log invariants** — `scripts/
  invariants_check.ps1` extracts the `_api_settings_state`
  function body and fails the build if any
  `logger.*(api_key|Authorization|body|...)` call is found.
- **45 new Python tests** in 3 new files:
  - `tests/chat/test_secret_source_v160.py` (17 tests)
  - `tests/chat/test_provider_state_v160.py` (12 tests)
  - `tests/security/test_ingress_scrubber_v160.py` (12 tests)
- **22 new v1.6.0 invariants** in
  `scripts/invariants_check.ps1` (SecretSource, ProviderState,
  settings/state route, IngressScrubber, source pill, test
  file existence).

What's new in v1.5.1.5 (over v1.5.1.4)
--------------------------------------
- **C7 streaming timeouts (defense in depth)** — the 2026-09-09
  live verification (docs/verification/2026-09-09-openrouter-probe.md)
  reported a "C7 dispatch hang on mock-llm/default". Root
  cause was a test-client bug (it awaited a HELLO frame the
  server never sends), not a C7 regression. v1.5.1.5 ships
  the defense-in-depth fix: explicit per-stream timeouts on
  both the mock (5s) and live (30s) dispatch paths. A future
  regression that deadlocks the streaming iterator now
  surfaces as `asyncio.TimeoutError` and is mapped by the
  WS handler in c1_gui_web_core to `chat.error` /
  `code: "stream_timeout"`. Constructor accepts
  `stream_timeout_s` (default 30.0) and `mock_stream_timeout_s`
  (default 5.0); callers can override per-call via
  `chat_stream(..., stream_timeout_s=...)`.
- **Source-level log invariants for the no-leak contract** —
  the verification also noted that no test pinned the absence
  of the API key or upstream response body from the
  `_api_llm_health_provider` handler's logs. v1.5.1.5 adds
  2 invariants that statically grep the handler body and
  fail the build if `logger.*(api_key|api_key_str|Authorization)`
  or `logger.*(resp.text|resp.content|body)` calls are found.
- **4 new Python tests** in
  `tests/chat/test_c7_stream_timeout_v1515.py`:
  - `test_v1515_mock_stream_raises_timeout`
  - `test_v1515_live_stream_raises_timeout`
  - `test_v1515_default_timeouts`
  - `test_v1515_chat_stream_accepts_per_call_cap`
- **14 new v1.5.1.5 invariants** in
  `scripts/invariants_check.ps1` (6 timeout contract, 1
  WS-handler stream_timeout mapping, 5 regression-test names,
  2 no-leak log invariants).
- **ADR-0108 v1.5.1.5 amendment** — documents the
  streaming timeout contract, the no-leak log discipline,
  and the corrected verification record (Finding #2 marked
  as FALSE POSITIVE — test-client bug, not a C7 regression).

What's new in v1.5.1.4 (over v1.5.1.3)
--------------------------------------
- **Real per-provider connection probe** — the v1.5.1
  `GET /api/llm/health` only confirmed the harness was
  started with `--llm-base-url`; it did NOT verify the
  user's OpenRouter key actually worked. v1.5.1.4 adds
  `GET /api/llm/health/{provider}` which uses
  OpenRouter's canonical `GET /api/v1/auth/key`
  endpoint — the documented "is my key valid" probe.
  Returns `{ok, label, limit, is_free_tier}` on 200,
  `{ok: false, error: "invalid_key"}` on 401, `{ok:
  false, error: "no_key"}` when no key is set, and
  `{ok: false, error: "network"}` on any other failure.
  The key is never echoed in the response body or
  logged.
- **"Test connection" button in the Settings modal** —
  each per-provider accordion has a Test connection
  button (rendered only when a key is stored) that calls
  the new probe and renders the result inline: green ✓
  with the OpenRouter label, red ✗ for invalid keys,
  amber for network errors, grey for not-supported
  providers.
- **Inline model picker in the chat header (no-session
  state)** — when no session is active, the chat header
  now renders a "Default model" picker. The user's pick
  is persisted in `dhc.defaultModelId` (localStorage)
  and sent as `{model: "..."}` in the POST body when
  creating a new session. The server validates the
  model id against the closed `ModelRegistry` (400 on
  unknown).
- **5 new Python tests** in
  `tests/chat/test_llm_health_v1514.py`:
  - `test_llm_health_openrouter_200_returns_label_and_limit`
  - `test_llm_health_openrouter_401_returns_invalid_key`
  - `test_llm_health_openrouter_no_key_returns_no_key`
  - `test_llm_health_openrouter_network_failure_returns_network`
  - `test_llm_health_openai_returns_not_supported`
- **4 new SettingsModal vitest tests** in a new
  `v1.5.1.4: per-provider probe` describe block.
- **1 new ChatPanel vitest test** in a new
  `v1.5.1.4: inline default model picker` describe block.
- **19 new v1.5.1.4 invariants** in
  `scripts/invariants_check.ps1` (route registered,
  handler uses /auth/key, 401 maps to invalid_key,
  test file + names, button + handler + fetch, picker
  + localStorage + POST body, server validation,
  vitest describe blocks).
- **ADR-0108 v1.5.1.4 amendment** — documents the
  per-provider probe, the inline picker, the server
  model validation, and the new tests.

What's new in v1.5.1.3 (over v1.5.1.2)
--------------------------------------
- **Settings "Save" returned 500 in the sanitized-error
  path (P0) fix** — when the secrets directory
  (`.dhc/secrets/`) was removed between server startup
  and the next PUT (e.g. the v1.5.1.2 smoke-test cleanup,
  a manual `rm`, or a migration that touched the dir),
  `PUT /api/secrets/{name}` and `DELETE /api/secrets/{name}`
  raised `FileNotFoundError: '.dhc\secrets\secrets.log'`.
  v1.5.1.2 added sanitized error messages on the
  frontend so the user saw a friendly "Server is busy.
  Try again or restart the server." — but the underlying
  500 was unfixed. The bug lived in
  `SecretsService.put_raw` and `SecretsService.delete`,
  both of which called `self._log.open("a", ...)` without
  ensuring the parent dir existed. The constructor's
  `mkdir` only ran at startup. v1.5.1.3 introduces
  `_append_log`, a helper that does
  `self._log.parent.mkdir(parents=True, exist_ok=True)`
  before every write, making the path robust to any
  post-startup state mutation. The helper runs inside
  the existing `self._lock` so concurrent PUTs from
  multiple tabs still serialize.
- **Audit closes the loop on the same class of bug as
  v1.5.1.2** — the v1.5.1.2 audit fixed
  `SessionManager._atomic_write_json` (the "click does
  nothing" session-create bug) but missed the parallel
  write path in `SecretsService`. v1.5.1.3 closes the
  loop. A grep across `src/dhc/**/*.py` for other
  `Path.open("a"|"w")` and `Path.write_*` call sites
  confirmed the only remaining write paths are either
  pre-mitigated with explicit
  `parent.mkdir(parents=True, exist_ok=True)` (e.g.
  `attachments.py:176`, `session_exporter_v1/service.py:41`)
  or are user-supplied (export paths).
- **5 new self-heal tests** in
  `tests/chat/test_secrets_self_heal_v1513.py`:
  - `test_put_raw_self_heals_missing_secrets_dir`
  - `test_delete_self_heals_missing_secrets_dir`
  - `test_self_heal_survives_full_state_wipe`
  - `test_self_heal_works_when_secrets_dir_never_existed_at_init`
  - `test_concurrent_puts_dont_interleave` (4 threads
    × 25 puts each, all 100 lines parse as JSON, no
    partial writes — pins the `self._lock` contract)
- **ADR-0108 v1.5.1.3 amendment** — documents the
  `_append_log` helper, the missing-parent-dir failure
  mode, and the 5 new tests that pin the contract.

What's new in v1.5.1.2 (over v1.5.1.1)
--------------------------------------
- **Session creation "click does nothing" (P0) fix** — `POST
  /api/sessions` could 500 with `FileNotFoundError` if the
  on-disk sessions directory was missing at write time. The
  `SessionManager` constructor's `mkdir` only ran at
  startup; any post-startup mutation left the next write
  hanging. v1.5.1.2 makes `_atomic_write_json` self-heal:
  it re-creates the parent directory on every write. The
  bug is reproduced by deleting `.dhc/sessions/sessions/`
  while the server is running and then `POST /api/sessions`.
  With the fix, the next write succeeds.
- **The hint no longer leaks the provider prefix** — the
  `first_3 + … + last_4` shape (e.g. `sk-…7890`) leaked
  the uniform `sk-` provider family. v1.5.1.2 changes the
  hint to `… + last_4` only (e.g. `…7890`). The modal's
  subtitle "Encrypted at rest, never sent to the browser
  in any response" is now literally true.
- **`fetchJson` no longer echoes the raw server body** — a
  shared helper at `apps/web/src/utils/sanitizeError.ts`
  maps 4xx/5xx to friendly text without echoing the raw
  body. Both `SettingsModal` and `ModelConfigMenu` use
  the shared helper. The raw body is still passed to the
  thrown `Error` as the `cause` for tests and devtools,
  but it is not interpolated into the user-visible string.
- **`newSession` no longer silently swallows 500s** — the
  React client now maps 4xx/5xx to friendly text and
  surfaces it inline in the rail below the "New chat"
  pill. The user can dismiss the error manually; it is
  also cleared on the next successful create.
- **CLI default for `--sessions-dir` is now the parent** —
  the previous default of `repo_root/.dhc/sessions` (the
  leaf) caused a doubled `sessions/sessions/` segment in
  the on-disk path. v1.5.1.2 flips the default to
  `repo_root/.dhc` (the parent that `SessionManager`
  expects). The 4 chat-ws test fixtures and the smoke
  runner were updated to pass `tmp_path` (the parent)
  directly.
- **ADR-0108 amendment** — the v1.5.1.2 amendment
  documents the hint redaction, the sanitized errors, the
  path self-heal, the new CLI default, and the inline
  newSession error UI.

What's new in v1.5.1.1 (over v1.5.1)
------------------------------------
- **Settings modal `PUT` 405 fix (P0)** — the v1.5.1 modal was
  firing `POST /api/secrets` with body `{name, value}`. The C1
  service registers `PUT /api/secrets/{name}` with body `{value}`.
  v1.5.1.1 fixes the frontend to fire the GitHub-shaped verb.
- **Metadata-only `GET /api/secrets`** — the response is now
  `{secrets: [{name, configured, updated_at, hint}], names}`.
  The full value is *never* returned. The `hint` is the first
  3 characters + `…` + last 4 (e.g. `sk-…7890`).
- **No-prefill input** — the per-row input is empty by default,
  even when the server confirms a key is stored. The eye toggle
  reveals the typed draft, not the stored value.
- **0600 fail-loud** — the secrets log is checked for `chmod 600`
  at startup. A permissive mode logs a warning and returns 403
  for PUT/DELETE until the user runs `chmod 600` and restarts.
- **Accordion default-state** — when the modal is opened from
  the welcome view (no active session), all four accordions
  collapse to the "Not set" pill. When opened from an active
  session, only that session's provider is expanded.
- **ADR-0108** — the key management contract that documents the
  GitHub-shaped `PUT`/`DELETE` verbs, the metadata-only `GET`,
  the no-prefill UX, and the 0600 fail-loud check.

What's new in v1.5.1 (over v1.5.0)
-----------------------------------
- **GUI redesign: "Redesigned Product"** — the chat panel
  is now a professional AI chat interface (Qwen / ChatGPT /
  Claude style), closing the seam between "redesigned chat"
  and "redesigned product".
  - **WelcomeView** in the no-session state: DHC monogram +
    24 px greeting "How can I help?" + 2×2 grid of suggestion
    cards. The same component renders in the no-session state
    AND the empty-active-session state, so the "Click + New
    session" rail that's visible in v1.5.0 no longer shows.
  - **Mixed 2+2 suggestion cards**: 2 conversation
    starters (Run an eval, Configure a model) that dispatch
    a prompt, and 2 action cards (Open a session, Manage
    API keys) that perform the action. The grid is
    visually uniform; the difference is in the verb
    (Ask vs. Open) and the subtitle.
  - **Auto-create on first intent**: clicking a
    conversation card or pressing Enter on an empty input
    auto-creates a session via POST /api/sessions, then
    dispatches the prompt. The in-flight promise ref
    (`newSessionInFlight`) is single-flight — double-
    clicking during the create round-trip doesn't create
    two sessions.
  - **Copy discipline**: "Mock LLM is connected" → "Ready"
    (3 states: Ready / Offline / Ready to help). "Message
    the harness…" → "Message Qwen…". Token counter
    hidden in general view (gated by `developerMode`).
  - **Developer Mode toggle**: the gear menu now
    has a styled switch (native checkbox replaced).
    Label changed from "v1.5.0 tools (branch, fork,
    tree)" to "Developer tools". The v1.5.0 features
    (BranchButton on every message, LeafSwitcher in
    the header, Tree button) are gated behind this
    toggle. Default is OFF; the choice persists in
    `localStorage["dhc.developerMode"]`.
  - **Sidebar restyle**: full-width "New chat" pill at
    the top (replaces the inline + button). Row actions
    (pin / archive / delete) reveal on hover/focus only.
    Pin icon visible only when `pinned === true`.
    Empty untitled sessions (message_count == 0 AND
    title is empty/default) are filtered out at the
    rail (display only — never server-side deleted).
    The active session is never pruned.
  - **Settings modal restyle**: per-provider accordions
    (OpenAI / Anthropic / OpenRouter / Mock, with
    `displayNameFor()` mapping). Each accordion's
    header shows a "Configured" / "Not set" pill
    (replaces the v1.5.0 per-row "Key saved" badge).
    Default key on top; per-model overrides collapsed
    in a nested `<details>`. Show/hide secret toggle
    (eye icon) per row, default hidden. Save is
    enabled only when the draft is non-empty AND dirty
    (draft != currentSaved, or unsaved). Dark inputs
    with monospace, the 70ch intro paragraph, the
    close button (SVG icon) in the top-right of the
    modal header. Portal to `document.body` so the
    chat shell doesn't bleed under the modal.
  - **ModalPortal component**: small wrapper that
    portals a modal to `document.body` with Escape
    close, simple focus trap, and focus return.
    SettingsModal and ModelConfigMenu both go
    through it.
  - **Stop / cancel while streaming**: a styled
    Stop button morphs into the Send button when
    streaming. Clicking Stop sends a `chat.cancel`
    over the WebSocket. The server-side handler
    acknowledges with `chat.cancelled`. The session
    manager exposes `cancel_stream(sid)`,
    `is_stream_cancelled(sid)`, and `clear_cancel(sid)`.
    The assistant message gets a `cancelled: true`
    field and a `cancelled` chip in the bubble's
    meta line. (See ADR-0022 for the mid-stream
    interrupt limitation: aiohttp's
    WebSocketResponse enforces "one pending receive()
    at a time" via `_waiting`, so the chat handler
    cannot race the outer `async for msg in ws:`
    loop. v1.5.1 implements the noop-ack path
    cleanly and the SessionManager cancel flag;
    mid-stream interrupt is v1.5.2 work.)
  - **Scroll pinning**: a sticky "Jump to latest"
    pill at the bottom-center of the messages
    container when the user scrolls up; click
    scrolls to the bottom and re-pins.
  - **Streaming bubble ARIA**: `aria-live="polite"`
    on the streaming content so screen readers
    announce the assistant's progress.
  - **`prefers-reduced-motion`**: the cursor blink
    is replaced with a static block, hover
    transforms are removed.
  - **Media queries**: the sidebar collapses to a
    56 px icon rail under 640 px; the welcome grid
    collapses to a single column under 640 px.
  - **One icon set**: all emoji replaced with
    inline-SVG icons in `apps/web/src/components/
    icons.tsx` (Lucide-style: 1.5 px stroke,
    `currentColor`, 24×24 viewBox). Names:
    plus, search, pin, archive, trash, gear, key,
    model, tree, fork, paperPlane, attach,
    chevronDown, chevronRight, eye, eyeOff, close,
    stop, arrowDown, play, logo, cancelled, running.
  - **Plugin memory** (carried from v1.5.0.1): the
    auto-load list persists in
    `~/.dhc/auto_load_plugins.json` via a new
    `PluginMemory` class (atomic via `*.tmp` +
    `os.replace`). Plugins can be enabled/disabled
    from the Modules tab; the choice survives
    restarts.
- **ADR-0022 (cancel flag round-trip)**: documented
  the mid-stream cancel limitation in a new ADR
  and added a unit test for the SessionManager
  cancel flag machinery.
- 16/16 DoD checks pass: pytest 845, vitest 73,
  invariants all pass, DHC-V 100.0, 19/19 v1.2.0 chat
  smoke, zip contents clean (no runtime artifacts, all
  36 doc files, SHA-pinning consistent, no staging
  prefix, entropy map marker, capability bijection,
  prompt-path C7 doesn't import number_gate,
  attachment ref invisible to key_lookup, all
  v1.2.x+v1.3.0+v1.3.1+v1.3.2+v1.3.3+v1.3.4+v1.4.0
  +v1.5.0+v1.5.1 source files present).

What's in v1.5.0 (carried forward)
-----------------------------------
- **Branching on the existing `m_<12 hex>` schema**
  (ADR-0019): the session message ledger is now a tree, not a
  list. Three new capabilities land:
  - `BRANCH_CREATE` (POST /api/sessions/{sid}/branches):
    forks a new branch from a parent message.
  - `BRANCH_SWITCH` (PATCH /api/sessions/{sid}/active-tip):
    sets the session's `active_tip_id`. The new name (not
    `active_leaf_id`) is the honest one: a *tip* is the
    message the user is extending; a *leaf* is any
    node without children.
  - `BRANCH_LIST` (GET /api/sessions/{sid}/branches):
    returns one entry per tip with its reconstructed
    path from the root.
  - `reconstruct_path(sid, leaf_id)` walks leaf → root
    with a max-depth guard of 256 (`BranchDepthExceeded`).
  - **Stream pinning**: an in-flight assistant message's
    `parent_id` is captured at the start of the stream
    and pinned, regardless of intervening branch
    switches. The server-side `stream_in_progress` flag
    refuses `branch_switch` with 409 while a stream is
    active. The frontend lockout is a v1.5.1 nicety.
  - `usage_totals` remains the branch-agnostic sum
    across all messages (no per-branch partitioning in
    v1.5.0; that's a v1.6.0 metric).
  - Old session files load unchanged: `parent_id` and
    `active_tip_id` are backfilled by `Session.from_dict`.

What's new in v1.5.0 (over v1.4.0)
-----------------------------------
- **Branching on the existing `m_<12 hex>` schema**

What's new in v1.5.0 (over v1.4.0)
-----------------------------------
- **Branching on the existing `m_<12 hex>` schema**
  (ADR-0019): the session message ledger is now a tree, not a
  list. Three new capabilities land:
  - `BRANCH_CREATE` (POST /api/sessions/{sid}/branches):
    forks a new branch from a parent message.
  - `BRANCH_SWITCH` (PATCH /api/sessions/{sid}/active-tip):
    sets the session's `active_tip_id`. The new name (not
    `active_leaf_id`) is the honest one: a *tip* is the
    message the user is extending; a *leaf* is any
    node without children.
  - `BRANCH_LIST` (GET /api/sessions/{sid}/branches):
    returns one entry per tip with its reconstructed
    path from the root.
  - `reconstruct_path(sid, leaf_id)` walks leaf → root
    with a max-depth guard of 256 (`BranchDepthExceeded`).
  - **Stream pinning**: an in-flight assistant message's
    `parent_id` is captured at the start of the stream
    and pinned, regardless of intervening branch
    switches. The server-side `stream_in_progress` flag
    refuses `branch_switch` with 409 while a stream is
    active. The frontend lockout is a v1.5.1 nicety.
  - `usage_totals` remains the branch-agnostic sum
    across all messages (no per-branch partitioning in
    v1.5.0; that's a v1.6.0 metric).
  - Old session files load unchanged: `parent_id` and
    `active_tip_id` are backfilled by `Session.from_dict`.

- **Attachments on the v0x04 envelope** (ADR-0020):
  user file uploads with routing by MIME class.
  - **Inline** for `text/plain`, `text/markdown`,
    `image/svg+xml`: the body is a UTF-8 string in
    `Message.attachments: list[InlineAttachment]`. The
    audit log projects the body through the 62-gate.
  - **Out-of-line** for `image/png`, `image/jpeg`,
    `image/gif`, `image/webp`, `audio/mpeg`,
    `audio/wav`, `application/pdf`: stored under
    `~/.dhc/attachments/{session_id}/{uuid}.bin` and
    wrapped in a v0x04 envelope with name
    `attachment:{session_id}:{uuid}`. The message
    carries only the ref.
  - **MIME allowlist**: exactly the 10 values in
    ADR-0020. Anything else is rejected with 415.
  - **Size limits**: 10 MB per file, 25 MB per message.
  - **Name binding** (ADR-0013 + ADR-0014): the
    `session_id` is part of the envelope name, so a
    ref from session A cannot be opened in session B
    without breaking the MAC. The `attachment:` prefix
    keeps the namespace invisible to `key_lookup`'s
    `llm_provider_` filter.
  - Three new C1 routes, all gated: `ATTACHMENT_PUT`,
    `ATTACHMENT_GET`, `ATTACHMENT_DELETE`.

- **Filter domains** (ADR-0021): a single document
  that names the four byte-flow categories in the
  system and declares the filter rule for each:
  - `audit-text` — 62-gate, escape non-legal. C2
    log emit, Entropy Map, secret names, capability
    names, file paths, manifest fields.
  - `asset` — no filter. Attachment binaries, `data:`
    URIs, envelope ciphertext on disk, in-memory
    `SessionEvent.payload`.
  - `provider-bytes` — lossless via
    `from_codes(strict=False)` if needed. Provider
    HTTP body, API key bytes.
  - `prompt-text` — no filter, governed by C3+C9+
    length cap. User message content, chat.send WS
    frame, C3 user section.

- **Prompt-path control** (ADR-0018): an explicit
  scope line on what the 62-gate does *not* defend.
  The 62-gate is a machine-interpretation boundary
  filter; it is not a prompt-injection defense. The
  prompt path is governed by C3 boundary-token
  escape + C9 agent-tool authorization + length cap
  + usage totals. A new test
  (`tests/cordis/test_prompt_path_unfiltered.py`)
  asserts the property positively.

- **Cap-to-32 amendment** (ADR-0015-amendment-1):
  the v1.4.0 cap of 24 is preserved as a frozen
  archaeology constant (`CAPABILITY_BUDGET_V140`).
  The v1.5.0 cap is 32 (`CAPABILITY_BUDGET_V150`).
  6 new capabilities land: 3 branching + 3
  attachments. The cap is enforced by
  `scripts/invariants_check.ps1` as a property
  check, not only by the module-load `assert`.

- **Bijection invariant** (Phase 1, Findings 1+2):
  `GuiWebCore` exposes
  `self.registered_capabilities: set[Capability]`
  populated at registration time. The invariant
  asserts `set(Capability) == ACTION_WHITELIST.keys()
  == web_core.registered_capabilities`. A new
  capability without a route, or a route with a
  capability not in the whitelist, fails the DoD.

- 5 new ADRs: 0015-amendment-1 (cap→32), 0018
  (prompt-path), 0019 (branching), 0020
  (attachments), 0021 (filter domains).

What's in v1.4.0 (carried forward)
----------------------------------
- Project CREC — the Entropy Gate (ADR-0015, ADR-0016,
  ADR-0017). Capability whitelist + 62-code Number Gate
  + tests as error boundary + Entropy Map. 20
  capabilities, v1.4.0 cap of 24 (preserved as
  `CAPABILITY_BUDGET_V140`).

What's in v1.3.x (carried forward)
----------------------------------
- v0x01 / v0x02 / v0x03 / v0x04 envelope versions; v0x04
  default since v1.3.4 (canonical MAC input, ADR-0014).
- Secret name binding (ADR-0013): v0x03+ envelopes authenticate
  the secret name in the MAC input. Confused-deputy via name
  swap is closed.
- Per-model API keys (ADR-0007 amendment).
- `Session.usage_totals` aggregate.
- Token counter + context-window progress bar.

What's in v1.3.x (carried forward)
----------------------------------
- v0x03 secrets envelope (ADR-0012): a third envelope version,
  `DHC3`, with extension blocks between the header and the AEAD
  nonce. The first extension, type 0x0001, carries a 12-byte
  strict per-secret nonce. The scrypt KDF salt for v0x03 is the
  28-byte concatenation `strict_nonce (12) ‖ aead_nonce (16)` —
  two independent random per-secret values. v0x01 (`DHC1`) and
  v0x02 (`DHC2`) envelopes remain readable. Unknown extension
  types are silently skipped (forward compat for v1.3.3+).
- Per-model API keys (ADR-0007 amendment): the SettingsModal
  now renders one row per `(provider, model_id)` pair, instead
  of one row per provider. The v1.3.1 per-provider key is
  preserved as the default fallback for any model that doesn't
  have its own key. To support multiple keys for the same
  `(provider, model_id)` pair, a `___n` disambiguator suffix is
  appended (e.g. `___2`, `___3`); the canonical name (no suffix)
  is always the default. The C7 dispatch uses a new
  `dhc.integrations.key_lookup.lookup_api_key` helper that
  walks the per-model → ___N → per-provider fallback chain.
- `Session.usage_totals` field (v1.3.2 ADR): every session
  carries a usage aggregate
  `{"prompt_tokens", "completion_tokens", "total_tokens",
  "last_turn_prompt", "last_turn_completion"}`. The C1 WS chat
  handler and the C1 HTTP message endpoint both accumulate
  `StreamChunk.usage` into this field after each turn and
  persist it on the session.
- Token counter + context-window progress bar
  (`<TokenCounter/>`): "Tokens: 1,234 / 8,192" with the
  denominator from `model.context_length`; a thin bar fills
  green→yellow→red as the ratio climbs. Pure React, no
  third-party UI lib. Reads from the piggybacked `usage_totals`
  field on the WS `message` event.

What's in v1.3.x (carried forward)
-----------------------------------
- 4-tab web UI: Modules, Events, Prompts, Chat.
- Server-side chat sessions (SessionManager) and encrypted
  secrets (SecretsService).
- Mock LLM fixture at tests/fixtures/mock_llm.py.
- <Markdown/> component as the single owner of
  dangerouslySetInnerHTML (ADR-0005).
- /ws/chat, /api/sessions[/...], /api/secrets[/...], /api/llm/health.
- 5 SHA-pinned plugins under src/dhc/plugins/.
- 3 live LLM provider clients (OpenAI, Anthropic, OpenRouter)
  behind an LLMProvider abstract base, with a 3-attempt, 1 s/2 s
  linear retry policy (5xx + network errors only, no 4xx retry).
- ModelRegistry with 6 hardcoded models across 4 providers
  (mock-llm/default, openai/gpt-4o-mini, openai/gpt-4.1,
  anthropic/claude-3-5-sonnet-latest, anthropic/claude-3-5-haiku-latest,
  openrouter/auto).
- 2 new C1 routes: GET /api/models, GET /api/models/{id} (with
  regex constraint to allow slashes in the id).
- <ModelSelect> React component in the chat header, persisted
  per-session via PATCH /api/sessions/{id} with {"model": ...}.
- 19-check v1.2.0 chat smoke (smoke_v12.py).
- v1.3.1: per-secret nonce in the secrets envelope (ADR-0010).
- v1.3.1: per-session `ModelConfig` (ADR-0011) with C1 routes
  `GET /api/sessions/{id}/config` and `POST .../config`.
- v1.3.1: `<SettingsModal>` per-provider API key entry.
- v1.3.1: `<ModelConfigMenu>` per-session config sliders.
- v1.3.1: `StreamChunk.usage` field for token usage.

Module map
----------
C1  GuiWebCore           — aiohttp + WS + CSP + bearer token + marketplace routes + chat/sessions/secrets/LLM-health + /api/models routes + /api/sessions/{id}/config + ws/chat
C2  SessionEventLog      — append-only tuple history with branching
C3  PromptAssembler      — boundary tokens + capability-filtered tool schema
C4  ToolGuardPipeline    — strict pydantic schemas; BashInput.command is list[str]
C5  AgentRegistry        — HMAC-SHA256 manifests; 0x1F-separated canonical form
C6  TurnStepDriver       — waterfall + circuit breaker (max_steps=5)
C7  LLMStreamAdapter     — OpenAI-compatible SSE (GET + POST); 1 MiB buffer cap; v1.3.0 dispatch seam (model_registry + secrets_service); v1.3.1 config_store seam; v1.3.2 per-model key lookup
C8  WebhookDispatch      — HMAC verify + LRU nonce store + ±5 min window
C9  CapabilityPolicy     — deny-all default; intercepts tools/pre-execute
C10 ObservabilitySink    — recursive PII/secret scrubber

Envelope versions (ADR-0010 + ADR-0012 + ADR-0013 + ADR-0014)
-------------------------------------------------------------
  v0x01  (DHC1)  fixed scrypt salt           — v1.2.0/v1.3.0 writes (read-only)
  v0x02  (DHC2)  per-envelope nonce          — v1.3.1 writes (read-only)
  v0x03  (DHC3)  strict nonce + ext blocks   — v1.3.2/v1.3.3 writes (read-only)
                                              + v1.3.3 name binding (non-canonical)
  v0x04  (DHC4)  strict nonce + ext blocks   — v1.3.4+ writes (current default)
                                              + v1.3.3 name binding
                                              + v1.3.4 canonical MAC input
v0x01 and v0x02 layouts: `HEADER (4) || nonce (16) || ct (n) || tag (32)`.
v0x03 and v0x04 layouts: `HEADER (4) || ext_total (2) || exts (n) || nonce (16) || ct (n) || tag (32)`.
v0x04 MAC input: `header || ext_area || nonce || len(name):u32 BE || name` (v1.3.4+).
v0x03 MAC input: `header || ext_area || nonce || name` (v1.3.3, non-canonical, read-only).
v0x01 / v0x02 MAC input: `header || nonce || ct` (no name binding).
`open_envelope` dispatches on the header.

New in v1.3.0
--------------
src/dhc/integrations/
  __init__.py             provider_client_for(model) factory
  base.py                 LLMProvider ABC, RetryConfig, ProviderError
  openai_client.py        OpenAIClient (OpenAI SSE parser + retry loop)
  anthropic_client.py     AnthropicClient (Anthropic SSE parser + retry)
  openrouter_client.py    OpenRouterClient (OpenAI-compatible + retry)

src/dhc/services/
  model_registry.py       Model + ModelRegistry (6 hardcoded models)

New in v1.3.1
-------------
src/dhc/services/
  model_config.py         ModelConfig (frozen dataclass) +
                          ModelConfigStore (encrypted JSON in
                          SecretsService under
                          `model_config_{session_id}`)

New in v1.3.2
-------------
src/dhc/integrations/
  key_lookup.py           lookup_api_key(provider, model_id, getter)
                          walks the per-model → ___N → per-provider
                          fallback chain (ADR-0007 amendment)
src/dhc/cordis/secrets.py adds:
  HEADER_V3, EXT_STRICT_NONCE, STRICT_NONCE_LEN
  _pack_extension, _serialize_extensions, _parse_extensions
  v0x03 envelope: extension blocks between header and AEAD nonce
src/dhc/services/session_manager.py:
  Session.usage_totals (frozen dict) + SessionManager.update(...,
  usage_totals=...) for v1.3.2 accumulation

Web client
----------
4 tabs: Modules, Events, Prompts, Chat
  apps/web/src/App.tsx                       router + WS (4 tabs)
  apps/web/src/panels/ModulesPanel.tsx       module/plugin grid + paste-and-score
  apps/web/src/panels/EventsPanel.tsx        live event stream
  apps/web/src/panels/PromptsPanel.tsx       10 master prompts browser
  apps/web/src/panels/ChatPanel.tsx          chat (sessions, streaming, search, model select, config, keys, token counter)
  apps/web/src/panels/SessionList.tsx        left-rail session history
  apps/web/src/components/Markdown.tsx       <Markdown/> + <ToolResult/> (XSS guardrail)
  apps/web/src/components/ModelSelect.tsx    v1.3.0 model selector (ADR-0006)
  apps/web/src/components/ModelConfigMenu.tsx v1.3.1 per-session config sliders
  apps/web/src/components/SettingsModal.tsx   v1.3.2 per-model API key entry (ADR-0007 amendment)
  apps/web/src/components/TokenCounter.tsx    v1.3.2 token counter + context-window bar
  apps/web/src/components/SearchOverlay.tsx   Ctrl+K search modal
  apps/web/src/components/ModuleCard.tsx     shared module/plugin card
  apps/web/src/types/chat.ts                 shared types (Message, Session, ChatFrame)
  apps/web/src/sanitize.ts                   DOMPurify wrappers (renderMarkdown, renderToolResult)
  apps/web/src/__tests__/                    vitest component tests
    SettingsModal.test.tsx
    ModelConfigMenu.test.tsx
    TokenCounter.test.tsx
    smoke.test.ts

Plugin marketplace
------------------
5 bundled plugins under src/dhc/plugins/, each with manifest.json + service.py:
  rate_limiter_v1       7601a11d5c671fe6abb6bbed55b047a5f3c6d4c053eace5e93cfc8b35409d6d8
  session_exporter_v1   35e6985c9ee8dee4b47e49a01d4daa4f817d933cf3b09605ded754d1a2367c75
  model_router_v1       e459a03e6e469324b3d93889a1fddf8645d840d02bea21ea5c22a007b0be2fa2
  memory_store_v1       ccea4ff8e3d680f3a1bc76cc5fac8b6a837507a26663bb3203a4ac3b003d8b
  prompt_browser_v1     b33c95c21e7617e773c117a2e730b8cf87c82823bb96f60b61c044aa78f1df85

C1 routes
---------
v1.1.0 (unchanged):
  GET    /                       static index.html with embedded <meta name="dhc-token">
  GET    /healthz                modules + discovered plugins + loaded plugins
  GET    /ws                     WebSocket event stream (token in query, loopback origin)
  GET    /api/manifest           full manifest (modules + plugins)
  GET    /plugins                discovered vs loaded plugins
  POST   /plugins/{id}           load a plugin
  DELETE /plugins/{id}           unload a plugin
  GET    /prompts                list 10 master prompts (requires prompt_browser_v1)
  GET    /prompts/{key}          single prompt body
  POST   /api/eval               offline in-proc eval of pasted code

v1.2.0:
  WS     /ws/chat                chat request/response (bearer token + loopback origin)
  GET    /api/llm/health         LLM adapter reachability
  GET    /api/sessions           list session summaries (?q= full-text, ?search= legacy)
  POST   /api/sessions           create a session
  GET    /api/sessions/{id}      full session
  PATCH  /api/sessions/{id}      rename/pin/archive/tag/set-model
  DELETE /api/sessions/{id}      soft-delete (or ?hard=1 to remove file)
  POST   /api/sessions/{id}/messages  send a turn; returns assistant message
  GET    /api/secrets            list names only (NEVER values)
  PUT    /api/secrets/{name}     encrypt and store
  DELETE /api/secrets/{name}     remove

v1.3.0 (new):
  GET    /api/models             list 6 hardcoded models (ADR-0006)
  GET    /api/models/{id:.+}     fetch a single model

v1.3.1 (new):
  GET    /api/sessions/{id}/config   read per-session ModelConfig (ADR-0011)
  POST   /api/sessions/{id}/config   write per-session ModelConfig

v1.3.2 (new):
  (no new routes; per-model key lookup uses existing /api/secrets)
  The /api/sessions/{id} response now includes `usage_totals`.
  The /api/sessions/{id}/messages response now includes `usage_totals`.
  The /ws/chat `chat.done` frame now includes `usage_totals`.

Static verification (no Python needed)
--------------------------------------
scripts/static_check.ps1
scripts/c8_static_check.ps1
scripts/invariants_check.ps1
scripts/hmac_sanity_check.ps1
scripts/package_relay.ps1
scripts/dod_verify.ps1

Sync recipe (from a provisioned host with git + gh)
----------------------------------------------------
unzip harness_benchmark-v1.3.2-20260904.zip
cd harness_benchmark
git init
git add .
git commit -m "v1.3.2 reference implementation (DHC-V 100.0, v0x03 envelope + per-model keys + usage totals + token counter)"
git remote add origin git@github.com:zenithprotocol001/harness.git
git push -u origin main --force

Files excluded from this artifact
---------------------------------
serve_c1.port
serve_c1.token
serve_c1.log
serve_c1.err.log
demo_live.log
demo_live.err.log
heartbeat.log
heartbeat.err.log
heartbeat_demo.log
heartbeat_demo.err.log
heartbeat_long.log
heartbeat_long.err.log
__pycache__/
.pytest_cache/
*.pid
apps/web/node_modules/  (reinstalled via `npm install` in apps/web)
